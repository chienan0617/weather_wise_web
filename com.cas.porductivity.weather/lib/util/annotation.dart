class Method {
  final String message;

  const Method(this.message);
}

const method = Method('method function');

class UseIn {
  final String message;

  const UseIn(this.message);
}

const useIn = UseIn('use in function');

class Branch {
  final String from;

  const Branch(this.from);
}

class RegisterFirst {
  final String message;

  const RegisterFirst(this.message);
}

const registerFirst = RegisterFirst('register first message');